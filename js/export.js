window.TPP=window.TPP||{};TPP.exportPDF=async()=>{TPP.switchView("print");TPP.showProgress(5,"Preparing print layout…");TPP.renderPrintPreview();const s=TPP.readSettings(),{jsPDF}=window.jspdf,pdf=new jsPDF({orientation:s.sheet.h>=s.sheet.w?"portrait":"landscape",unit:"in",format:[s.sheet.w,s.sheet.h],compress:true}),sheets=[...document.querySelectorAll('[data-pdf-page="true"]')];for(let i=0;i<sheets.length;i++){TPP.showProgress(10+Math.round(i/sheets.length*80),`Rendering PDF page ${i+1} of ${sheets.length}…`);const sheet=sheets[i],ot=sheet.style.transform,om=sheet.style.marginBottom;sheet.style.transform="none";sheet.style.marginBottom="0";const canvas=await html2canvas(sheet,{scale:3,backgroundColor:"#ffffff"}),img=canvas.toDataURL("image/jpeg",.95);if(i>0)pdf.addPage([s.sheet.w,s.sheet.h]);pdf.addImage(img,"JPEG",0,0,s.sheet.w,s.sheet.h);sheet.style.transform=ot;sheet.style.marginBottom=om;await new Promise(requestAnimationFrame)}const safe=(s.title||"tiny-book").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");pdf.save(`${safe||"tiny-book"}.pdf`);TPP.showProgress(100,"PDF complete.")};
