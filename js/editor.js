window.TPP=window.TPP||{};TPP.populateSelects=()=>{fontFamily.innerHTML=TPP.fonts.map(([v,l])=>`<option value="${TPP.escapeAttr(v)}">${l}</option>`).join("");paperPreset.innerHTML=Object.entries(TPP.paperPresets).map(([v,p])=>`<option value="${v}">${p.label}</option>`).join("");texture.innerHTML=insideCoverTexture.innerHTML=Object.entries(TPP.textures).map(([v,l])=>`<option value="${v}">${l}</option>`).join("")};TPP.loadBookIntoForm=()=>{const b=TPP.activeBook;TPP.fields.forEach(id=>{const el=document.getElementById(id);if(!el)return;if(el.type==="checkbox")el.checked=!!b[id];else el.value=b[id]??""});document.querySelector(".custom-size").hidden=pageSize.value!=="custom";TPP.renderChapterEditor()};TPP.syncFormToBook=()=>{const b=TPP.activeBook;TPP.fields.forEach(id=>{const el=document.getElementById(id);if(!el)return;if(el.type==="checkbox")b[id]=el.checked;else if(el.type==="number"||el.type==="range")b[id]=+el.value;else b[id]=el.value});b.chapters=TPP.readChaptersFromEditor();TPP.saveLibrary()};TPP.renderChapterEditor=()=>{chaptersEditor.innerHTML=TPP.activeBook.chapters.map((ch,i)=>`<article class="chapter-card" data-chapter-id="${ch.id}"><div class="chapter-card-header"><h3>Chapter ${i+1}</h3><div class="chapter-actions"><button data-action="up">↑</button><button data-action="down">↓</button><button data-action="remove">Remove</button></div></div><label>Chapter Title <input class="chapter-title" value="${TPP.escapeAttr(ch.title||"")}"></label><div class="format-toolbar"><button class="small" data-format="bold" type="button">Bold</button><button class="small" data-format="italic" type="button">Italic</button><button class="small" data-format="underline" type="button">Underline</button><button class="small" data-format="strike" type="button">Strike</button><button class="small" data-format="ul" type="button">Bullets</button><button class="small" data-format="h2" type="button">Heading</button><button class="small" data-format="table" type="button">Table</button></div><div class="editor-split"><label>Markdown Text<textarea class="chapter-text" rows="12">${TPP.escapeHtml(ch.text||"")}</textarea></label><div><strong>Preview</strong><div class="rendered-markdown-preview">${TPP.safeMarkdown(ch.text||"")}</div></div></div><div class="two-col"><label>Image Placement<select class="chapter-image-placement"><option value="none" ${ch.imagePlacement==="none"?"selected":""}>No Image</option><option value="own-page" ${ch.imagePlacement==="own-page"?"selected":""}>Own Page</option><option value="below-title" ${ch.imagePlacement==="below-title"?"selected":""}>Below Title</option></select></label><label>Image Width (%) <input class="chapter-image-width" type="number" min="10" max="100" step="1" value="${ch.imageWidth??70}"></label></div><label>Chapter Image <input class="chapter-image" type="file" accept="image/*"></label>${ch.imageData?`<img src="${ch.imageData}" alt="" style="max-width:140px;border-radius:10px">`:""}</article>`).join("")};TPP.readChaptersFromEditor=()=>{const cards=[...document.querySelectorAll(".chapter-card")];if(!cards.length&&TPP.activeBook?.chapters)return TPP.activeBook.chapters;return cards.map(card=>{const old=TPP.activeBook.chapters.find(c=>c.id===card.dataset.chapterId)||{};return{id:card.dataset.chapterId||TPP.uid(),title:card.querySelector(".chapter-title").value,text:card.querySelector(".chapter-text").value,imageData:old.imageData||"",imagePlacement:card.querySelector(".chapter-image-placement").value,imageWidth:+card.querySelector(".chapter-image-width").value||70}})};TPP.updateMarkdownPreview=card=>card.querySelector(".rendered-markdown-preview").innerHTML=TPP.safeMarkdown(card.querySelector(".chapter-text").value);TPP.showProgress=(pct,text="Rendering…")=>{progressWrap.hidden=false;progressBar.style.width=`${Math.max(0,Math.min(100,pct))}%`;progressText.textContent=text;if(pct>=100)setTimeout(()=>progressWrap.hidden=true,500)};
